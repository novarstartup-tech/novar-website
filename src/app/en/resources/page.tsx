import { ResourcesView } from '@/components/MarketingPages';
export const metadata = { title: 'Resources', alternates: { canonical: '/en/resources' } };
export default function Page() { return <ResourcesView locale="en" />; }
